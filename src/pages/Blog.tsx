/* ... (previous content with import for OnboardingModal) */
// Update import and usage to: import { OnboardingModal } from "@/components/OnboardingModal";
// Then use <OnboardingModal isOpen={showOnboarding} onClose=... /> (replace inline version)